function solution(numbers, k) {
    var answer = 0;
    let o = 0;
    for (let i = 0; i < k; i++) {
        for (let j = 0; i < 2; j++) {
            if (o == numbers[numbers.length - 1])
        }
    }
    return answer;
}